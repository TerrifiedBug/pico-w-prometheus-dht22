secrets.py
internal-docs/

# Version file (branch-specific) - each branch maintains its own
version.txt

# Local configuration overrides (device-specific)
config.local.py

# Ensure version.txt stays separate between branches
version.txt
